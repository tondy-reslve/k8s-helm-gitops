apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: redis-sit
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "19"
spec:
  project: sit-project
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: my-app-sit
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
  source:
    repoURL: 'https://github.com/tondy-reslve/k8s-helm-gitops.git'
    targetRevision: HEAD
    path: charts/redis
    helm:
      valueFiles:
        - ../../middleware/redis/values-sit.yaml