# SPDX-License-Identifier: Apache-2.0
config:
  pgHbaEntries:
    - type: host
      database: app
      user: app
      address: 10.0.0.0/8
      method: scram-sha-256

standalone:
  persistence:
    enabled: false
