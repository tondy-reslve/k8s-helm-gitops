# SPDX-License-Identifier: Apache-2.0
architecture: replication

auth:
  enabled: true
  existingSecret: external-valkey-auth
  existingSecretPasswordKey: password

replication:
  replicaCount: 1

extraManifests:
  - apiVersion: v1
    kind: Secret
    metadata:
      name: external-valkey-auth
    type: Opaque
    stringData:
      password: ci-valkey-password
